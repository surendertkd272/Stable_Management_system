{
  "name": "bsv-equicare-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "EquiCare edge/cloud backend — ingests sensor readings, serves the SPA. Zero runtime dependencies.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node index.mjs",
    "dev": "node index.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
