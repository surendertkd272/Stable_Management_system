node_modules
dist
dist-ssr
.next
out
next-env.d.ts
.vercel
*.local
.DS_Store
.vite
npm-debug.log*
yarn-error.log*
*.tsbuildinfo

# EquiCare runtime artifacts
server/data/
edge/outbox*.jsonl
edge/outbox*.sending
edge/outbox*.lock
edge/edge_config.json
.env.local

# Vendor SDK: keep the documentation and sample source, exclude prebuilt
# binaries. They are x86-64 only (we need aarch64), are re-obtainable from the
# vendor, and would permanently bloat git history by ~50 MB.
SDK/**/*.so
SDK/**/*.dll
SDK/**/*.tar.gz
SDK/**/bin/
SDK/**/Warehouse/Release/
SDK/**/obj/

# large test media (sourced clips, not source code)
testdata/

# private vendor chat export (contains contact and GST details)
pdf/WhatsApp Chat*/
